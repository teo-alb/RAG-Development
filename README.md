##RAG HTML Loader demo
A minimal demonstration of Retrieval-Augmented-Generation (RAG) that loads the content from a website using 'AsyncHtmlLoader'.
This project is designed to be expanded into a totally functional RAG system.

##Features

- **Indexing Pipeline:**
 Loading the files -> Chunking -> Embedding -> Storing in Database
- **Generation Pipeline:**
 Retrieval -> Augmentation -> Generation (RAG)
- **Minimal dependencies:**
 Easy to set up and run
- **Scalable:**
 Modular design allows easy expansion
- Ready to expand to full working system